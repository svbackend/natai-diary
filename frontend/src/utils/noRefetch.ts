/** @typedef {import('react-query')} ReactQuery */
export const reactQueryNoRefetchOptions = {
    refetchOnWindowFocus: false,
    refetchOnMount: false,
    refetchOnReconnect: false,
    retry: false,
}
